node_modules/
dist/
.env
.env.local
.env.*.local
*.log
npm-debug.log*
.DS_Store
Thumbs.db
.vite/
coverage/

# Saída do `npm run types:db`. Serve para conferir o schema depois de uma
# migration; quem vale para a aplicação é src/types/database.ts.
supabase/database.gerado.ts

.vercel

# Cópia do repositório do iGreen Design System, usada só para consulta ao
# portar componentes. Tem .git próprio: sem esta linha, um `git add` nela
# gravaria um gitlink e criaria um submódulo quebrado apontando para um
# commit que não existe neste remoto.
meu-produto/
