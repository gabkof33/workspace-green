node_modules/
dist/
.env
.env.local
.env.*.local
*.log
npm-debug.log*
.DS_Store
Thumbs.db
.vite/
coverage/

# Saída do `npm run types:db`. Serve para conferir o schema depois de uma
# migration; quem vale para a aplicação é src/types/database.ts.
supabase/database.gerado.ts

.vercel

# O repositório do iGreen Design System mora FORA desta pasta, como irmã dela
# (../igreen-design-system). A linha fica como guarda: se alguém clonar o DS
# aqui dentro de novo, um `git add` gravaria um gitlink e criaria um submódulo
# quebrado, apontando para um commit que não existe neste remoto.
meu-produto/
igreen-design-system/
